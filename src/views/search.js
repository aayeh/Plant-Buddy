import React, { Fragment } from "react";

import { SearchContent } from "../components";

const Search = () => (
  <Fragment>
    <SearchContent />
    <hr />
  </Fragment>
);

export default Search;
